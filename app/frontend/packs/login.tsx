import React from 'react'
import ReactDOM from 'react-dom'

import Login from 'react/containers/Login';

document.addEventListener('DOMContentLoaded', () => {
  ReactDOM.render(
    <Login />,
    document.getElementById('root'),
  )
})
